<form action="{{ path('searchengine_search_cv_result') }}" method="post" {{ form_enctype(form) }} class="well form-search">
 
    <!-- form errors -->
    {{ form_errors(form) }}
 
        {{ form_errors(form.titre) }}
        {{ form_widget(form.titre) }}
 
    {{ form_rest(form) }}
 
    <input class="btn btn-primary clear" type="submit" />
</form>
