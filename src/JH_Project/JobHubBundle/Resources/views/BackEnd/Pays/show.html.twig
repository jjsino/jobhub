
{% extends "JobHubBundle::admin_layout.html.twig" %}


{% block page_title %}
<div class="navbar-header">
	<h2>Pays</h2>
</div>
<ul class="entity_actions pull-right back_to_list record_actions">
	<li>
		<a class="btn" href="{{ path('administration_pays') }}">
		    Retour à la liste
		</a>
	</li>
</ul>
{% endblock %}

{% block page_content %}
    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ entity.code }}</td>
            </tr>
            <tr>
                <th>Alpha2</th>
                <td>{{ entity.alpha2 }}</td>
            </tr>
            <tr>
                <th>Alpha3</th>
                <td>{{ entity.alpha3 }}</td>
            </tr>
            <tr>
                <th>Nomen</th>
                <td>{{ entity.nomEn }}</td>
            </tr>
            <tr>
                <th>Nomfr</th>
                <td>{{ entity.nomFr }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('administration_pays') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('administration_pays_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
